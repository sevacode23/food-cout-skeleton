// Throttler (request rate limiting) module configuration
// Use Redis for rate limiting for stateless architecture and scalability

import { Module } from '@nestjs/common';

@Module({})
export class ThrottlerModule {}
